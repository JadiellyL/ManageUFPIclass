
<%= form_for(course) do |f| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>
      
      <ul>
      <% course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <br>
  <br>
  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :name %>
  </div>

  <br>

  <div class="actions">
    <%= f.submit "Cadastrar", :class => 'btn btn-outline-primary'%> |
	<%= link_to 'Voltar', courses_path, :class => "btn btn-outline-secondary"%>
  </div>
<% end %>
