<%= form_for(reserva) do |f| %>
  <% if reserva.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reserva.errors.count, "error") %> prohibited this reserva from being saved:</h2>

      <ul>
      <% reserva.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :user_id %><br>
  <%= f.collection_select(:user_id, User.all, :id, :name, {:prompt=>true}, {:class=>'ui search dropdown'}) %>

  <%= f.label :sala_id %><br>
  <%= f.collection_select(:sala_id, Sala.all, :id, :number, {:prompt=>true}, {:class=>'ui search dropdown'}) %>

  <div class="field">
    <%= f.label :time_input %>
    <%= f.time_select :time_input %>
  </div>

  <div class="field">
    <%= f.label :time_output %>
    <%= f.time_select :time_output %>
  </div>

  <div class="field">
    <%= f.label :date %>
    <%= f.date_select :date %>
  </div>

  <div class="field">
    <%= f.label :quantity %>
    <%= f.number_field :quantity %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
