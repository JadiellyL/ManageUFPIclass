
<%= form_for(student) do |f| %>
  <% if student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :matricula %>
    <%= f.text_field :matricula %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :cpf %>
    <%= f.text_field :cpf %>
  </div>

  <div class="field">
    <%= f.label :telefone %>
    <%= f.text_field :telefone %>
  </div>

  <div class="field">
    <%= f.label :curso %>
    <%= f.collection_select(:curso, Course.all, :id, :name, {:prompt => 'Selecionar Curso'}, {:class=>'Colocar classe CSS'}) %>
  </div>

  <div class="field">
  <%= f.label :sexo %>
    <%= f.select(:sexo, options_for_select(['Masculino', 'Feminino']), {:prompt => 'Selecionar Gênero'}, {:class => ""}) %>
  </div>
     
  <div class="actions">
  
    <%= f.submit "Cadastrar", :class => "btn btn-outline-primary"%> |
    <%= link_to 'Mostrar', student, :class => "btn btn-outline-info"%> |
    <%= link_to 'Voltar', students_path, :class => "btn btn-outline-secondary"%>
    
  </div>
  
<% end %>
